"""
Generates many queries automatically from a source. Generated queries are compatible
with the selected databased and their results are noted stored for comparisson by other modules. 
"""
# create table telco_customer (customerID varchar(10), gender varchar(100), SeniorCitizen boolean, Partner varchar(10), Dependents varchar(10), tenure int, PhoneService varchar(100), MultipleLines varchar(100), InternetService varchar(255), OnlineSecurity varchar(100), OnlineBackup varchar(100), DeviceProtection varchar(100), TechSupport varchar(100), StreamingTV varchar(100), StreamingMovies varchar(100), Contract varchar(255), PaperlessBilling boolean, PaymentMethod varchar(255), MonthlyCharges float, TotalCharges float, Churn boolean);

import sqlparse